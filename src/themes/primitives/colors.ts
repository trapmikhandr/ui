/**
 * COLOR PRIMITIVES
 *
 * Export the selected color palette.
 *
 * To change the palette, update the import:
 * import { ocean as colors } from './color-presets'  // Teal
 * import { blue as colors } from './color-presets'   // Classic Blue
 * import { violet as colors } from './color-presets' // Purple
 * import { rose as colors } from './color-presets'   // Pink
 * import { amber as colors } from './color-presets'  // Warm Orange
 */

// Select the palette here.
// Export all presets so consumers can choose one.
export { green as colors } from "./material-pressets";
