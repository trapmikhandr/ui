/**
 * CONTRACTS
 *
 * Contracts define the token structure without values.
 *
 * colorContract — changes between light and dark themes.
 * globalContract — remains the same across themes.
 */

export { type ColorContract, colorContract } from "./color.contract.css";
export { type DensityContract, densityContract } from "./density.contract.css";
export { type GlobalContract, globalContract } from "./global.contract.css";
